# Pi Analytics Dashboard Ansible Inventory Template
# Copy this file to inventory.ini and edit with your Raspberry Pi details
# The inventory.ini file is gitignored to keep your settings private

[pi]
# Configure your Raspberry Pi connection(s) below
# You can deploy to MULTIPLE Pis simultaneously by adding multiple lines!

# === SINGLE PI SETUP ===
# Uncomment and edit ONE of these lines for a single Pi:

# Option 1: Using hostname with password
#raspberrypi.local ansible_user=pi ansible_ssh_pass=CHANGE_ME

# Option 2: Using IP address with password  
#192.168.1.100 ansible_user=pi ansible_ssh_pass=CHANGE_ME

# Option 3: Using SSH key (recommended for security)
#192.168.1.100 ansible_user=pi ansible_ssh_private_key_file=~/.ssh/id_rsa

# === MULTIPLE PI SETUP ===
# Deploy to multiple Pis at once! Uncomment and edit these lines:
# Each Pi will get the same application but with its own configuration

# Example: Deploy to 3 Pis simultaneously
#pi-display-1.local ansible_user=pi ansible_ssh_pass=CHANGE_ME
#pi-display-2.local ansible_user=pi ansible_ssh_pass=CHANGE_ME  
#pi-display-3.local ansible_user=pi ansible_ssh_pass=CHANGE_ME

# Or using IP addresses for multiple Pis:
#192.168.1.101 ansible_user=pi ansible_ssh_pass=password1
#192.168.1.102 ansible_user=pi ansible_ssh_pass=password2
#192.168.1.103 ansible_user=pi ansible_ssh_pass=password3

# Mix and match - some with passwords, some with SSH keys:
#office-pi.local ansible_user=pi ansible_ssh_private_key_file=~/.ssh/id_rsa
#lobby-pi.local ansible_user=pi ansible_ssh_pass=CHANGE_ME
#conference-pi.local ansible_user=pi ansible_ssh_private_key_file=~/.ssh/pi_key

# IMPORTANT: Uncomment and edit the lines for YOUR Pi(s) above!

[pi:vars]
# Python interpreter on Raspberry Pi
ansible_python_interpreter=/usr/bin/python3

# ============================================================================
# NOTE: Most configuration can be changed later via the web interface!
# After deployment, access the config page at: http://your-pi-ip/config
# Or press Ctrl+Shift+C on the display to open the configuration interface
#
# Only hardware-specific settings (pi_model, display_type) must be set here
# Everything else (WiFi, themes, metrics, OTA settings) can be configured
# through the web UI after installation
# ============================================================================

# REQUIRED: Pi model configuration (cannot be changed via web UI)
# Options: pi_zero_w, pi_zero_2w, pi3, pi4, pi5
pi_model=pi_zero_w

# REQUIRED: Display configuration (cannot be changed via web UI)
# Options: hyperpixel_round, waveshare_34_hdmi, hdmi
display_type=hyperpixel_round

# Application installation directory (optional - defaults to user home)
# app_dir=/home/pi/pi-analytics-dashboard

# WiFi Configuration (optional - can be changed later via web UI)
# wifi_ssid=YourWiFiNetwork
# wifi_password=YourWiFiPassword
# wifi_country=US

# OTA Update Configuration (optional - can be changed later via web UI)
# ota_enabled=true
# ota_branch=main
# ota_check_on_boot=true
# ota_auto_pull=false